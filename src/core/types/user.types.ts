/** Re-export barrel from features/profile. */
export { UserRole, UserState } from "@/src/features/profile/types/user.types"
export type { UserStats, FeaturedPost, FavoriteGame, User } from "@/src/features/profile/types/user.types"
